# coding=utf-8


def acv_logit(w, X, Ycode):
    """An approximate leave-one-out estimator of predictive likelihood
     for logistic regression with l1 regularization[1]

    Compute and return an approximate leave-one-out estimator (LOOE) and
    its standard error of predivtive likelihood for logistic regression
    penalized by l1 norm.

    Args:
        w:
        X:
        Ycode:

    Returns:
        LOOE, ERR (float, float)

    References:
        [1]: T. Obuchi and Y. Kabashima, XXXXXXXXXXXX
    """
    LOOE = 0.0
    ERR = 0.0

    return LOOE, ERR
